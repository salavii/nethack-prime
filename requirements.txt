# Python 3.11 only -- NLE publishes wheels up to cp311. On newer Python,
# pip falls back to a full source build (cmake, bison, flex).
nle>=1.3.0
gymnasium>=1.2.0

# The driver loop talks to the model over stdlib urllib; no HTTP client needed.
